name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      # 전체 사이트 배포 — 메인(알약케이스) + 구독 페이지 + 위젯
      - name: Assemble site
        run: |
          mkdir -p _site/css _site/js
          cp index.html subscribe.html widget.js _site/
          cp css/style.css css/subscribe.css _site/css/
          cp js/main.js js/subscribe.js _site/js/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
